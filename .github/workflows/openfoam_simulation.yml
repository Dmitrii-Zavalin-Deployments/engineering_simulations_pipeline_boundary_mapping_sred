name: OpenFOAM Simulation
on:
  push:
    branches:
      - '**'  # Runs on push to any branch

jobs:
  install-openfoam:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install Required Dependencies
        run: |
          sudo apt update
          sudo apt install -y software-properties-common wget

      - name: Add OpenFOAM Repository & Install OpenFOAM
        run: |
          sudo wget -O - https://dl.openfoam.com/add-debian-repo.sh | sudo bash
          sudo apt update
          sudo apt install -y openfoam2306  # Ensure this matches the latest available version

      - name: Verify OpenFOAM Installation
        run: |
          dpkg -l | grep openfoam || (echo "Error: OpenFOAM installation failed!" && exit 1)

      - name: Locate OpenFOAM Environment and Source It
        run: |
          # Use OpenFOAM selector tool to activate the correct version
          OPENFOAM_VERSION=$(openfoam-selector --list | tail -n 1)

          if [ -n "$OPENFOAM_VERSION" ]; then
            source /usr/lib/openfoam/$OPENFOAM_VERSION/etc/bashrc
            echo "OpenFOAM version $OPENFOAM_VERSION sourced successfully!"
          else
            echo "Error: OpenFOAM version not found! Checking alternative locations..."
            exit 1
          fi

      - name: Verify OpenFOAM Setup
        run: |
          foamVersion || (echo "Error: OpenFOAM verification failed!" && exit 1)
          echo "OpenFOAM installation verified!"
